# 河海大学 2012 级硕士研究生《数学物理方程》期末试卷题目

> 考试时间：2013.1.7
> 试卷来源：数理方程12-20级期末试卷.pdf 第54-55页
> OCR 工具：pix2text 1.1.6（公式识别模型 mfr-1.5 + 版面分析 doclayout-yolo）
>
> ⚠️ **此试卷在 PDF 中仅有 2 页，内容不完整**（可见部分约 40 分），其余题目可能未被扫描录入。

---

## 一、选择题（4 × 3 = 12 分）

### 1.（3分）

偏微分方程与（ ）结合在一起，称为初值问题。

A. 定解条件　B. 初始条件　C. 边界条件　D. 以上都对

### 2.（3分）

微分方程定解问题称为适定的，是指（ ）。

A. 存在唯一的解　B. 存在稳定的解　C. 存在唯一且稳定的解　D. 以上都不对

### 3.（3分）

二阶线性偏微分方程 $u_{xx} + 5u_{xy} + 6x u_x + y u_y = 0$ 是（ ）类型。

A. 双曲型　B. 抛物型　C. 椭圆型　D. 混合型

### 4.（3分）

二维 Laplace 方程 $u_{xx} + u_{yy} = 0$ 的一个解是（ ）。

A. $u = e^x \sin 2y$　B. $u = \sqrt{x^2 + y^2}$　C. $u = \dfrac{1}{\sqrt{x^2 + y^2}}$　D. $u = \ln\sqrt{x^2 + y^2}$

---

## 二、填空题（共 18 分）

### 1.（4分）

设 $\mu_i$（$i = 1, 2, \ldots$）是零阶 Bessel 函数 $J_0(x)$（$0 < x < a$）的正实零点，则当 $i \neq j$ 时，

$$\int_0^a x\,J_0\!\left(\frac{\mu_i}{a}x\right) J_0\!\left(\frac{\mu_j}{a}x\right) dx = \_\_\_\_\_\_$$

当 $i = j$ 时，

$$\int_0^a x\,J_0^2\!\left(\frac{\mu_i}{a}x\right) dx = \_\_\_\_\_\_$$

### 2.（4分）

设函数 $u$ 在区域 $\Omega \subset \mathbb{R}^3$ 内是调和的，$M_0$ 是 $\Omega$ 内的任意一点，$B_a$ 表示以 $M_0$ 为中心、以 $a$ 为半径的球域，且 $B_a \subset \Omega$，$\partial B_a$ 表示 $B_a$ 的边界。则

$$u(M_0) = \_\_\_\_\_\_$$

### 3.（4分）

设 $f(x)$、$\cos x$ 的 Fourier 变换分别为 $\mathcal{F}[f(x)] = F(\alpha)$、$\mathcal{F}[\cos x] = G(\alpha)$，则 $\mathcal{F}^{-1}[F(\alpha)\,G(\alpha)] =$ \_\_\_\_\_\_。

### 4.（6分）

函数 $t^n e^{-at}$（$n \geq 1$，$n$ 为正整数）的 Laplace 变换为 \_\_\_\_\_\_；函数 $\dfrac{s^2}{(s^2 + \omega^2)^2}$ 的 Laplace 逆变换为 \_\_\_\_\_\_。

---

## 三、（10 分）行波法求解初值问题

用行波法求解下列初值问题：

$$\begin{cases} u_{xx} + 3u_{xy} - 4u_{yy} = 0, & -\infty < x < \infty,\ y > 0, \\ u(x,0) = e^{2x},\quad u_y(x,0) = 1, & -\infty < x < \infty. \end{cases}$$

---

> ⚠️ **此试卷不完整**：PDF 中仅含 2 页，可见部分总分约 40 分（12 + 18 + 10），其余题目未被扫描录入。完整的期末试卷总分应为 100 分。

---

## 修订记录

> 本文件基于 **pix2text 1.1.6**（mfr-1.5 公式识别模型）重新 OCR 生成。

### 与旧版（tesseract OCR）的关键差异

| 题号 | 旧版（tesseract） | 新版（pix2text） | 置信度 |
| ---- | ----------------- | ---------------- | ------ |
| 一.3 | 方程系数未能识别 | $u_{xx} + 5u_{xy} + 6x u_x + y u_y = 0$ | ✅ 高 |
| 一.4 | $u = e^x\sin y$（选项 A） | $u = e^x\sin 2y$ | ✅ 高 |
| 一.4 | $u = 1/r$（选项 C） | $u = 1/\sqrt{x^2+y^2}$ | ✅ 高 — 等价的 |
| 二.4 | $t^n$ 的 Laplace 变换 | $t^n e^{-at}$ 的 Laplace 变换 | ✅ 高 — 含频移 |
| 三 | $u(x,0) = e^{x^2}$ | $u(x,0) = e^{2x}$ | ✅ 高 |

### 保留不变的部分

| 题号 | 内容 | 说明 |
| ---- | ---- | ---- |
| 一.1 | 初值问题定义 | 两版一致 |
| 一.2 | 适定性定义 | 两版一致 |
| 二.1~3 | Bessel 正交性/平均值/Fourier 卷积 | 两版一致 |
| 三 | 行波法方法 | 两版一致 |

> ⚠️ 此试卷仍需在找到完整扫描件后补充其余题目。
