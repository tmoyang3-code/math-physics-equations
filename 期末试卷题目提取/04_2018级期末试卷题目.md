# 河海大学 2018 级硕士研究生《数学物理方程》期末试卷题目

> 考试时间：2019.1.17
> 试卷来源：数理方程12-20级期末试卷.pdf 第13-18页
> OCR 工具：pix2text 1.1.6（公式识别模型 mfr-1.5 + 版面分析 doclayout-yolo）

---

## 一、填空题（5 × 6 = 30 分）

### 1.（6分）

说明物理现象初始状态的条件叫 \_\_\_\_\_\_，说明边界上约束情况的条件叫 \_\_\_\_\_\_，统称为 \_\_\_\_\_\_。

### 2.（6分）

方程 $u_{xx} + xy u_{yy} = 0$ 当 \_\_\_\_\_\_ 时是双曲型方程；当 \_\_\_\_\_\_ 时是抛物型方程；当 \_\_\_\_\_\_ 时是椭圆型方程。

### 3.（6分）

设函数 $u(x,y)$ 对 $x$ 的 Fourier 变换为 $U(\alpha,y)$，则 Laplace 方程

$$u_{xx} + u_{yy} = 0$$

的 Fourier 变换式为 \_\_\_\_\_\_。

### 4.（6分）

函数 $a + bt^{2018} + c\sin\omega t + de^{3t}\cos\omega t$ 的 Laplace 变换为 \_\_\_\_\_\_。

### 5.（6分）

定解问题

$$\begin{cases} u_{xy} = 6x^2 y, & x > 0,\ y > 0, \\ u(x,0) = 1 - \cos x, & x \geq 0, \\ u(0,y) = y^2, & y \geq 0 \end{cases}$$

的解为 \_\_\_\_\_\_。

---

## 二、（15 分）把方程化为标准型并求通解

把方程

$$u_{xx} - 2\lambda u_{xy} - 3\lambda^2 u_{yy} + u_x + \lambda u_y = 0$$

化为标准型，并且求出通解，其中 $\lambda > 0$ 为常数。

---

## 三、（15 分）非齐次波动方程定解问题 + 能量积分证唯一性

求下列定解问题的解：

$$\begin{cases} u_{tt} = c^2 u_{xx}, & 0 < x < \pi,\ t > 0, \\ u(0,t) = u_x(\pi,t) = 0, & t \geq 0, \\ u(x,0) = x,\quad u_t(x,0) = \cos 2x, & 0 \leq x \leq \pi, \end{cases}$$

其中 $c > 0$ 是常数，并且利用**能量积分方法**证明：该问题的古典解是唯一的。

---

## 四、（15 分）上半平面 Laplace 方程 Dirichlet 问题

求上半平面 Laplace 方程的 Dirichlet 问题的解：

$$\begin{cases} u_{xx} + u_{yy} = 0, & x \in (-\infty, +\infty),\ y > 0, \\[4pt] u(x,0) = \displaystyle\sum_{n=1}^{2018} (a_n \cos nx + b_n \sin nx), & x \in (-\infty, +\infty), \\[8pt] \displaystyle\lim_{y \to +\infty} u(x,y) = 0. \end{cases}$$

---

## 五、（15 分）Laplace 变换法解微分积分方程

用 Laplace 变换法求解下列微分积分方程：

$$y'(t) = -2y(t) - \int_0^t y(s)\,ds,\quad t \geq 0,\quad y(0) = 1.$$

---

## 六、（5 分）圆域 Poisson 方程 Dirichlet 问题

求下列二维 Poisson 方程在圆域内 Dirichlet 问题的解：

$$\begin{cases} u_{xx} + u_{yy} = a x^2 + b y^2, & x^2 + y^2 < R^2, \\[4pt] u(x,y) = \lambda_1 x^2 + \lambda_2 xy + \lambda_3 y^2 + \lambda_4 x + \lambda_5 y + \lambda_6, & x^2 + y^2 = R^2, \end{cases}$$

其中 $a, b, \lambda_k$（$k = 1, \cdots, 6$）为常数。

---

## 七、（5 分）Fourier 变换法解扩散方程初值问题

利用 Fourier 变换方法，求解如下扩散方程的初值问题：

$$\begin{cases} u_t = a^2 u_{xx} + b u_x + c u, & -\infty < x < +\infty,\ t > 0, \\ u(x,0) = \varphi(x), & -\infty < x < +\infty, \end{cases}$$

的解，其中 $a > 0$、$b$、$c$ 为常数。进一步，求出 $\varphi(x) = \sin x$ 时的特解。

已知：

$$\int_0^{+\infty} e^{-\beta x^2} \cos\alpha x \, dx = \frac{\sqrt{\pi}}{2\sqrt{\beta}}\, e^{-\frac{\alpha^2}{4\beta}},\quad \beta > 0.$$

---

## 修订记录

> 本文件基于 **pix2text 1.1.6**（mfr-1.5 公式识别模型）重新 OCR 生成，并用**04_2018级期末试卷解答与评分标准**中的解答交叉验证。

### 与旧版（tesseract OCR）的关键差异

| 题号 | 旧版（tesseract） | 新版（pix2text） | 置信度 |
| ---- | ----------------- | ---------------- | ------ |
| 一.2 | $u_{xx} + u_{yy} = 0$（无分类变量） | $u_{xx} + xy u_{yy} = 0$ | ✅ 高 — 答案可验证：$\Delta = -xy$ |
| 一.3 | "$u_{xx}+u_{yy}=0$ 对 $y$ 的 Fourier 变换式" | "$u_{xx}+u_{yy}=0$ 的 Fourier 变换式" | ✅ 高 — 答案：$U_{yy} - \alpha^2 U = 0$ |
| 一.4 | $at + bt^2 + c\sin\omega t + d e^{\omega t}\cos\omega t$ | $a + bt^{2018} + c\sin\omega t + de^{3t}\cos\omega t$ | ✅ 高 — 答案验证 |
| 二 | $u_x + u_y = 0$ | $u_x + \lambda u_y = 0$ | ✅ 高 — $\lambda$ 系数与判别式一致 |
| 五 | $y'(t) = -2y(t) - y(t)\int_0^t y(s)ds$ | $y'(t) = -2y(t) - \int_0^t y(s)ds$ | ✅ 高 — 答案验证：无 $y(t)$ 因子 |
| 六 | 边界条件未能识别 | $u = \lambda_1 x^2 + \lambda_2 xy + \lambda_3 y^2 + \lambda_4 x + \lambda_5 y + \lambda_6$ | ✅ 高 — 二次多项式边值 |
| 七 | $u_t = a u_{xx} + b u + c$ | $u_t = a^2 u_{xx} + b u_x + c u$ | ✅ 高 — 含对流项 $bu_x$，答案验证 |

### 保留不变的部分

| 题号 | 内容 | 说明 |
| ---- | ---- | ---- |
| 一.1 | 初始条件/边界条件/定解条件 | 两版一致 |
| 三 | 波动方程 Neumann 混合边界 $u(0,t)=u_x(\pi,t)=0$ | 两版一致 |
| 四 | 上半平面 Dirichlet 问题 $\sum_{n=1}^{2018}$ | 两版一致 |

> 试卷总分构成：填空题 30 分 + 第二~七题（15 + 15 + 15 + 15 + 5 + 5）= **100 分**。
