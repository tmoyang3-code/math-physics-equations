# 2023级《数学物理方程》期末试卷题目

---

## 一、填空题（共12空，每空2分，共24分）

1. 偏微分方程定解问题的适定性包括解的稳定性、$\underline{\qquad}$和$\underline{\qquad}$。
2. 波动方程、热传导方程、位势方程，它们依次是$\underline{\qquad}$型、$\underline{\qquad}$型、$\underline{\qquad}$型方程最典型的代表。
3. 极小曲面方程 $(1+u_y^2)u_{xx} - 2u_xu_yu_{xy} + (1+u_x^2)u_{yy} = 0$ 的阶数是$\underline{\qquad}$。
4. 二阶线性偏微分方程 $5u_{xx} + 4u_{xy} - 3u_{xz} + 2u_{yy} - u_{zz} + u_x + u_y = 0$ 的特征方程为$\underline{\qquad}$。
5. 设 $f(x) = \begin{cases} e^{ax}, & |x| \leq a \\ 0, & |x| > a > 0 \end{cases}$，则 $\mathcal{F}[f(x)] = \underline{\qquad}$。
6. 设 $\mathcal{F}[f(x)] = F(\alpha)$，$\mathcal{F}[\sin x] = G(\alpha)$，则 $\mathcal{F}^{-1}[F(\alpha)G(\alpha)] = \underline{\qquad}$。
7. $\mathcal{L}[e^{-at}\cos bt] = \underline{\qquad}$，$\mathcal{L}^{-1}\left[\dfrac{1}{s(s^2+1)}\right] = \underline{\qquad}$。
8. 在 Laplace 方程 $u_{xx} + u_{yy} = 0$ 两边关于 $x$ 做 Fourier 变换，得到常微分方程是$\underline{\qquad}$。

---

## 二、解答题（共5题，每题12分，共60分）

### 1. 化简二阶线性偏微分方程为标准型

化简二阶线性偏微分方程：

$$
x^2u_{xx} + 2xyu_{xy} + y^2u_{yy} + xyu_x + y^2u_y = 0
$$

为标准型。

---

### 2. 利用 Kirchhoff 公式求解波动方程初值问题

利用 Kirchhoff 公式求解波动方程初值问题：

$$
\begin{cases}
u_{tt} = u_{xx} + t\cos x, & -\infty < x < +\infty, \ t > 0 \\
u(x,0) = 1 + x^2, \ u_t(x,0) = \cos x, & -\infty < x < +\infty
\end{cases}
$$

---

### 3. 用分离变量法求解扇形区域上拉普拉斯方程边值问题

用分离变量法求解扇形区域上拉普拉斯方程边值问题：

$$
\begin{cases}
\Delta u = u_{rr} + r^{-1}u_r + r^{-2}u_{\theta\theta} = 0, & r < 1, \ 0 < \theta < \dfrac{\pi}{4} \\
u(r,0) = 0, \quad u_\theta\left(r,\dfrac{\pi}{4}\right)=0, & 0 \leq r \leq 1 \\
u(1,\theta) = \sin 2\theta + \sin 6\theta, & 0 \leq \theta \leq \dfrac{\pi}{4}
\end{cases}
$$

---

### 4. 用特征函数法求解热传导方程混合问题

用特征函数法求解热传导方程混合问题：

$$
\begin{cases}
u_t = u_{xx} + \sin\dfrac{x}{2}, & 0 < x < \pi, \ t > 0 \\
u(x,0) = \sin\dfrac{x}{2}, & 0 \leq x \leq \pi \\
u(0,t) = 0, \ u_x(\pi,t) = 0, & t \geq 0
\end{cases}
$$

---

### 5. 用 Laplace 变换法求解波动方程半无界定解问题

用 Laplace 变换法求解波动方程半无界定解问题：

$$
\begin{cases}
u_{tt} = a^2u_{xx}, & x > 0, \ t > 0 \\
u(x,0) = 0, \ u_t(x,0) = 0, & x \geq 0 \\
u(0,t) = f(t), \ \lim\limits_{x \to +\infty} u(x,t) = 0, & t \geq 0
\end{cases}
$$

---

## 三、证明题（本题16分）

利用能量积分方法，证明下述初边值问题解的唯一性：

$$
\begin{cases}
u_{tt} = a^2u_{xx} - bu_t + h(x,t), & 0 < x < L, \ t > 0 \\
u(x,0) = \varphi(x), \ u_t(x,0) = \psi(x), & 0 \leq x \leq L \\
(-\alpha u_x + \beta u)|_{x=0} = p(t), & t \geq 0 \\
(\alpha u_x + \beta u)|_{x=L} = q(t), & t \geq 0
\end{cases}
$$

其中常数 $\alpha, \beta > 0$。该问题的能量积分定义为：

$$
E(t) = \frac{1}{2}\int_0^L (u_t^2 + a^2u_x^2)dx + \frac{\beta a^2}{\alpha}(u^2(L,t) + u^2(0,t))
$$
