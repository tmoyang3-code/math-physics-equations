# 河海大学 2015 级硕士研究生《数学物理方程》期末试卷题目

> 考试时间：2016.1.15
> 试卷来源：数理方程12-20级期末试卷.pdf 第36-41页
> OCR 工具：pix2text 1.1.6（公式识别模型 mfr-1.5 + 版面分析 doclayout-yolo）

---

## 一、填空题（6 × 5 = 30 分）

### 1.（5分）

二阶线性齐次偏微分方程如果没有给定解的条件，一般有 \_\_\_\_\_\_ 个解；偏微分方程定解问题的适定性是指解的 \_\_\_\_\_\_。

### 2.（5分）

分段函数

$$f(t) = \begin{cases} 1, & 0 < t < 1, \\ -1, & -1 < t < 0, \\ 0, & |t| > 1 \end{cases}$$

的 Fourier 变换是 \_\_\_\_\_\_。

### 3.（5分）

函数 $1 + t^3 + \cos t + te^t \sin 2t$ 的 Laplace 变换是 \_\_\_\_\_\_。

### 4.（5分）

写出一维波动方程初值问题

$$\begin{cases} u_{tt} = a^2 u_{xx} + h(x,t), & -\infty < x < \infty,\ t > 0, \\ u(x,0) = f(x),\quad u_t(x,0) = g(x), & -\infty < x < \infty \end{cases}$$

的求解公式：\_\_\_\_\_\_

### 5.（5分）

定解问题

$$\begin{cases} u_{xy} = x^2 y, & x > 0,\ y > 0, \\ u(x,0) = \sin x, & x \geq 0, \\ u(0,y) = y^2, & y \geq 0 \end{cases}$$

的解是 \_\_\_\_\_\_。

### 6.（5分）

设 $\Omega$ 是 $\mathbb{R}^3$ 中有界区域，边界 $\partial\Omega$ 光滑或者分片光滑，函数 $u, v \in C^2(\Omega) \cap C(\overline{\Omega})$，写出关于函数 $u, v$ 的第一格林公式和第二格林公式。

---

## 二、（10 分）求方程的通解

求下列方程的通解：

$$u_{xx} + 2u_{xy} - 3u_{yy} = 4(u_x - u_y).$$

---

## 三、（12 分）波动方程 Dirichlet 边值定解问题

求下列定解问题的解：

$$\begin{cases} u_{tt} = a^2 u_{xx}, & 0 < x < \pi,\ t > 0, \\ u(x,0) = x,\quad u_t(x,0) = \sin 4x, & 0 \leq x \leq \pi, \\ u(0,t) = u(\pi,t) = 0, & t \geq 0. \end{cases}$$

---

## 四、（12 分）圆域 Dirichlet 问题

求解圆域上 Dirichlet（狄利克雷）问题的解：

$$\begin{cases} u_{rr} + r^{-1} u_r + r^{-2} u_{\theta\theta} = r\sin\theta, & 0 < r < a,\ 0 \leq \theta \leq 2\pi, \\ u(a,\theta) = A\sin 4\theta + B\cos 6\theta, & 0 \leq \theta \leq 2\pi, \end{cases}$$

其中 $A, B$ 为常数。

---

## 五、（12 分）Laplace 变换法求解波动方程

用 Laplace 变换法求解下列定解问题：

$$\begin{cases} u_{tt} = a^2 u_{xx} + a^2 \sin\omega t, & x > 0,\ t > 0, \\ u(x,0) = u_t(x,0) = 0, & x \geq 0, \\ u(0,t) = 0, & t \geq 0, \\ u(x,t) \text{ 有界当 } x \to +\infty. \end{cases}$$

---

## 六、（12 分）Fourier 变换法求解热传导方程初值问题

用 Fourier 变换方法求解下列初值问题：

$$\begin{cases} u_t = c^2 u_{xx}, & -\infty < x < \infty,\ t > 0, \\ u(x,0) = x^2 + \cos x, & -\infty < x < \infty. \end{cases}$$

已知：若 $b > 0$，则

$$\int_0^\infty e^{-b x^2} \cos tx \, dx = \frac{\sqrt{\pi}}{2\sqrt{b}}\, e^{-\frac{t^2}{4b}}.$$

---

## 七、（12 分）极值原理或能量积分法证唯一性

用极值原理或者能量积分方法证明初边值问题

$$\begin{cases} u_t = c^2 u_{xx} + f(x,t), & 0 < x < L,\ t > 0, \\ u(x,0) = \varphi(x), & 0 \leq x \leq L, \\ -u_x(0,t) + \alpha u(0,t) = g(t), & t \geq 0, \\ u_x(L,t) + \alpha u(L,t) = h(t), & t \geq 0, \end{cases}$$

的古典（光滑）解若存在，则必唯一，其中常数 $\alpha > 0$。

---

## 修订记录

> 本文件基于 **pix2text 1.1.6**（mfr-1.5 公式识别模型）重新 OCR 生成。

### 与旧版（tesseract OCR）的关键差异

| 题号 | 旧版（tesseract） | 新版（pix2text） | 置信度 |
| ---- | ----------------- | ---------------- | ------ |
| 一.3 | $1 + t^3 + \cos t + t e^t \sin 2t$ | 相同，确认 | ✅ 高 |
| 一.5 | $u_{xx}+u_{yy}=0$, $u(x,0)=\sin x$, $u(0,y)=y$ | $u_{xy}=x^2y$, $u(x,0)=\sin x$, $u(0,y)=y^2$ | ✅ 高 — 方程类型完全不同 |
| 二 | $u_{xx}+2u_{xy}-5u_{yy}=\lambda(u_x-u_y)$ | $u_{xx}+2u_{xy}-3u_{yy}=4(u_x-u_y)$ | ✅ 高 — 系数不同 |
| 三 | $u_x(0,t)=u_x(\pi,t)=0$（Neumann） | $u(0,t)=u(\pi,t)=0$（**Dirichlet**） | ✅ 高 — 边界条件类型不同 |
| 四 | 源项 $r\sin\theta$ | 相同，确认 | ✅ 高 |
| 五 | $u_{tt}=c^2 u_{xx}+a^2\sin\omega t$ | $u_{tt}=a^2 u_{xx}+a^2\sin\omega t$ | ✅ 高 — 系数 $a^2$ 确认 |
| 六 | $u(x,0) = x^2 + \cos x$ | 相同，确认 | ✅ 高 |
| 七 | $u_{tt}=c^2 u_{xx}+f$（波动方程） | $u_t=c^2 u_{xx}+f$（**热传导方程**） | ✅ 高 — 方程类型不同 |

### 保留不变的部分

| 题号 | 内容 | 说明 |
| ---- | ---- | ---- |
| 一.1 | 适定性定义 | 两版一致 |
| 一.2 | 分段函数 Fourier 变换 | 两版一致 |
| 一.4 | d'Alembert 公式 | 两版一致 |
| 一.6 | 格林公式 | 两版一致 |

> 试卷总分构成：填空题 30 分 + 第二~七题（10 + 12 + 12 + 12 + 12 + 12）= **100 分**。
