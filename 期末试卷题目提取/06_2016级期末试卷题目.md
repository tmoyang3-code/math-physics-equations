# 河海大学 2016 级硕士研究生《数学物理方程》期末试卷题目

> 考试时间：2017.1.10
> 试卷来源：数理方程12-20级期末试卷.pdf 第30-35页
> OCR 工具：pix2text 1.1.6（公式识别模型 mfr-1.5 + 版面分析 doclayout-yolo）

---

## 一、填空题（6 × 5 = 30 分）

### 1.（5分）

二阶线性齐次偏微分方程如果没有给定解的条件，一般有 \_\_\_\_\_\_ 个解；偏微分方程定解问题的适定性是指解的 \_\_\_\_\_\_。

### 2.（5分）

分段函数

$$f(t) = \begin{cases} -1, & 0 < t < 1, \\ 1, & -1 < t < 0, \\ 0, & |t| > 1 \end{cases}$$

的 Fourier 变换是 \_\_\_\_\_\_。

### 3.（5分）

函数 $1 + t^2 + \sin t + te^t \cos 2t$ 的 Laplace 变换是 \_\_\_\_\_\_。

### 4.（5分）

写出一维波动方程初值问题

$$\begin{cases} u_{tt} = a^2 u_{xx} + h(x,t), & -\infty < x < \infty,\ t > 0, \\ u(x,0) = f(x),\quad u_t(x,0) = g(x), & -\infty < x < \infty \end{cases}$$

的求解公式：\_\_\_\_\_\_

### 5.（5分）

如果 $f(x)$ 是已知连续函数，那么方程 $u_{xy} - \lambda u_x = f(x)$ 的通解是 \_\_\_\_\_\_。

### 6.（5分）

设 $\Omega$ 是 $\mathbb{R}^3$ 中有界区域，边界 $\partial\Omega$ 光滑或者分片光滑，函数 $u, v \in C^2(\Omega) \cap C(\overline{\Omega})$，写出关于函数 $u, v$ 的第一格林公式和第二格林公式。

---

## 二、（10 分）求方程的通解

求下列方程的通解：

$$x^2 u_{xx} + 2xy u_{xy} + y^2 u_{yy} = 0.$$

---

## 三、（12 分）波动方程 Neumann 边值定解问题

求下列定解问题的解：

$$\begin{cases} u_{tt} = a^2 u_{xx}, & 0 < x < \pi,\ t > 0, \\ u(x,0) = x,\quad u_t(x,0) = \sin 4x, & 0 \leq x \leq \pi, \\ u_x(0,t) = u_x(\pi,t) = 0, & t \geq 0. \end{cases}$$

---

## 四、（12 分）半无限条形区域 Laplace 方程估计

设 $\Omega = (0,1) \times (0,+\infty)$，$u = u(x,y) \in C^2(\Omega) \cap C(\bar\Omega)$ 在 $\Omega$ 上有界，$|u(x,y)| \leq A$，且满足

$$\begin{cases} \Delta u = u_{xx} + u_{yy} = 0, & (x,y) \in \Omega, \\ u(0,y) = u(1,y) = 0, & y \geq 0. \end{cases}$$

证明：

$$|u(x,y)| \leq \frac{2A e^{-\pi y}}{1 - e^{-\pi y}},\quad \forall (x,y) \in \Omega.$$

（提示：用特征函数展开方法求出其解，再进行估计。）

---

## 五、（12 分）Laplace 变换法求解波动方程

用 Laplace 变换法求解下列定解问题：

$$\begin{cases} u_{tt} = a^2 (u_{xx} + \cos\omega t), & x > 0,\ t > 0, \\ u(x,0) = u_t(x,0) = 0, & x \geq 0, \\ u(0,t) = 0, & t \geq 0, \\ u(x,t) \text{ 有界当 } x \to +\infty. \end{cases}$$

---

## 六、（12 分）Laplace 方程 Neumann 问题可解条件

证明 Laplace 方程第二边值问题（Neumann 问题）

$$\begin{cases} u_{xx} + u_{yy} = 0, & 0 < x < a,\ 0 < y < b, \\ u_x(0,y) = f_1(y),\quad u_x(a,y) = f_2(y), & 0 \leq y \leq b, \\ u_y(x,0) = g_1(x),\quad u_y(x,b) = g_2(x), & 0 \leq x \leq a \end{cases}$$

有解的必要条件是函数 $f_1, f_2, g_1, g_2$ 满足相容性条件：

$$\int_0^a [g_1(x) - g_2(x)]\,dx + \int_0^b [f_1(y) - f_2(y)]\,dy = 0.$$

---

## 七、（12 分）能量积分法证明抛物型方程唯一性

利用能量积分方法，证明抛物型方程的初边值问题

$$\begin{cases} u_t = c^2(u_{xx} + u_{yy}) - bu + f(x,y,t), & (x,y) \in \Omega,\ t > 0, \\ u(x,y,0) = \varphi(x,y), & (x,y) \in \bar\Omega, \\[4pt] \alpha u + \beta\dfrac{\partial u}{\partial n} = \psi(x,y,t), & (x,y) \in \partial\Omega,\ t \geq 0, \end{cases}$$

的古典（光滑）解若存在，则必唯一。其中常数 $c, \alpha, \beta > 0$，$\Omega$ 是 $xy$ 平面上的有界区域，边界 $\partial\Omega$ 光滑，$n$ 是 $\partial\Omega$ 的单位外法线方向。

---

## 修订记录

> 本文件基于 **pix2text 1.1.6**（mfr-1.5 公式识别模型）重新 OCR 生成。

### 与旧版（tesseract OCR）的关键差异

| 题号 | 旧版（tesseract） | 新版（pix2text） | 置信度 |
| ---- | ----------------- | ---------------- | ------ |
| 一.2 | $f(t) = \begin{cases} 1, & -1<t<0 \\ -1, & 0<t<1 \end{cases}$ | $f(t) = \begin{cases} -1, & 0<t<1 \\ 1, & -1<t<0 \end{cases}$ | ✅ 高 — 符号反转 |
| 一.3 | $1 + t^2 + \sin t + t e^t \cos 2t$ | 相同，确认 | ✅ 高 |
| 一.5 | $u_{xx} - \lambda u = f(x)$ | $u_{xy} - \lambda u_x = f(x)$ | ✅ 高 — 不同方程类型 |
| 二 | $u_{xx} + 2\sqrt{y}u_{xy} + y u_{yy} = 0$ | $x^2 u_{xx} + 2xy u_{xy} + y^2 u_{yy} = 0$ | ✅ 高 — Euler 方程 |
| 三 | $u(x,0)$ 未能识别 | $u(x,0) = x$ | ✅ 高 |
| 三 | $u_t(x,0) = \sin x$ | $u_t(x,0) = \sin 4x$ | ✅ 高 |
| 五 | $u_{tt} = a^2 u_{xx} + \cos\omega t$ | $u_{tt} = a^2(u_{xx} + \cos\omega t)$ | ⚠️ 中 — 括号差异，建议对照 |
| 六 | 相容条件未能完整识别 | $\int_0^a[g_1-g_2]dx + \int_0^b[f_1-f_2]dy=0$ | ✅ 高 |
| 七 | BC: $\frac{\partial u}{\partial n} = \psi$（Neumann） | BC: $\alpha u + \beta\frac{\partial u}{\partial n} = \psi$（Robin） | ✅ 高 |

### 保留不变的部分

| 题号 | 内容 | 说明 |
| ---- | ---- | ---- |
| 一.1 | 适定性定义 | 两版一致 |
| 一.4 | d'Alembert 公式 | 两版一致 |
| 一.6 | 格林公式 | 两版一致 |
| 四 | 半无限条形域估计 | 两版一致 |

> 试卷总分构成：填空题 30 分 + 第二~七题（10 + 12 + 12 + 12 + 12 + 12）= **100 分**。
