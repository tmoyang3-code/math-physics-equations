# 河海大学 2013 级硕士研究生《数学物理方程》期末试卷题目

> 考试时间：2014.1.13
> 试卷来源：数理方程12-20级期末试卷.pdf 第48-53页
> OCR 工具：pix2text 1.1.6（公式识别模型 mfr-1.5 + 版面分析 doclayout-yolo）

---

## 一、填空题（6 × 5 = 30 分）

### 1.（5分）

在平面极坐标下，二维拉普拉斯方程可以表示为 \_\_\_\_\_\_。

### 2.（5分）

特征值问题

$$\begin{cases} X''(x) + \lambda X(x) = 0, & 0 < x < L, \\ X'(0) = X'(L) = 0 \end{cases}$$

其对应的特征函数为 \_\_\_\_\_\_。

### 3.（5分）

设 $\Omega \subset \mathbb{R}^3$ 中的有界区域，边界 $\partial\Omega$ 光滑。那么三维拉普拉斯方程第二边值内问题

$$\begin{cases} \Delta u = 0, & \text{in } \Omega, \\[4pt] \dfrac{\partial u}{\partial n} = f, & \text{on } \partial\Omega \end{cases}$$

有解的必要条件是 \_\_\_\_\_\_。

### 4.（5分）⚠️

方程 $u_{xx} = xy$ 的类型是 \_\_\_\_\_\_。

### 5.（5分）

Bessel 方程 $x^2 y'' + xy' + (x^2 - 3)y = 0$ 的通解是 \_\_\_\_\_\_。

### 6.（5分）

一维波动方程初值问题

$$\begin{cases} u_{tt} = a^2 u_{xx} + e^x, & -\infty < x < \infty,\ t > 0, \\ u(x,0) = 5,\quad u_t(x,0) = x^2, & -\infty < x < \infty \end{cases}$$

的解为 \_\_\_\_\_\_。

---

## 二、（10 分）方程化标准型

把方程

$$u_{xx} - 3u_{xy} - 4u_{yy} - u_x - 2u_y = 0$$

化为标准型。

---

## 三、（10 分）热传导方程求解 + 证唯一性

先求下列定解问题的解，然后证明该定解问题的解是唯一的：

$$\begin{cases} u_t = u_{xx} - 2, & 0 < x < 1,\ t > 0, \\ u(0,t) = 0,\quad u(1,t) = 1, & t > 0, \\ u(x,0) = x^2, & 0 \leq x \leq 1. \end{cases}$$

---

## 四、（10 分）波动方程非齐次源项

求解下列定解问题：

$$\begin{cases} u_{tt} = a^2 u_{xx} + t\sin\dfrac{\pi x}{L}, & 0 < x < L,\ t > 0, \\ u(0,t) = u(L,t) = 0, & t \geq 0, \\ u(x,0) = 0,\quad u_t(x,0) = 0, & 0 \leq x \leq L. \end{cases}$$

---

## 五、（10 分）圆域 Poisson 方程 Dirichlet 问题

求解圆域上狄利克雷（Dirichlet）问题的解：

$$\begin{cases} \Delta u = u_{xx} + u_{yy} = -4, & x^2 + y^2 < 1, \\ u(x,y) = 0, & x^2 + y^2 = 1. \end{cases}$$

---

## 六、（10 分）Fourier 变换法求解热传导方程

用傅里叶（Fourier）变换方法求解下列定解问题：

$$\begin{cases} u_t = c^2 u_{xx}, & -\infty < x < \infty,\ t > 0, \\ u(x,0) = 1 + \cos x, & -\infty < x < \infty. \end{cases}$$

已知：

$$\mathcal{F}^{-1}\big[e^{-c^2 \alpha^2 t}\big] = \frac{1}{2c\sqrt{\pi t}}\, e^{-\frac{x^2}{4c^2 t}}.$$

---

## 七、（10 分）Laplace 变换法求解 ODE

用 Laplace 变换求解：

$$\begin{cases} y''(t) - 3y'(t) + 2y(t) = e^t \sin t, & t > 0, \\ y(0) = -3,\quad y'(0) = 5. \end{cases}$$

---

## 八、（10 分）圆柱区域 Laplace 方程边值问题

求解圆柱区域的边值问题：在圆柱内 $\Delta u = 0$，在圆柱下底 $u|_{z=0} = 0$，在上底 $u|_{z=L} = A$，在圆柱侧面 $u|_{r=a} = 0$。

即求下列边值问题的解 $u = u(r,\theta,z)$：

$$\begin{cases} \Delta u = u_{rr} + r^{-1} u_r + r^{-2} u_{\theta\theta} + u_{zz} = 0, & 0 < r < a,\ 0 \leq \theta < 2\pi,\ 0 < z < L, \\ u(r,\theta,0) = 0,\quad u(r,\theta,L) = A, & 0 < r < a,\ 0 \leq \theta < 2\pi, \\ u(a,\theta,z) = 0, & 0 \leq \theta < 2\pi,\ 0 \leq z \leq L. \end{cases}$$

---

## 修订记录

> 本文件基于 **pix2text 1.1.6**（mfr-1.5 公式识别模型）重新 OCR 生成。

### 与旧版（tesseract OCR）的关键差异

| 题号 | 旧版（tesseract） | 新版（pix2text） | 置信度 |
| ---- | ----------------- | ---------------- | ------ |
| 一.4 | 未能识别 | $u_{xx} = xy$ 的类型判别 | ✅ 高 — 已人工对照纸质试卷确认 |
| 一.6 | $u(x,0)$ 和 $u_t(x,0)$ 未能完整识别 | $u(x,0)=5$, $u_t(x,0)=x^2$, 源项 $e^x$ | ✅ 高 |
| 二 | $u_{xx} - \beta u_{xy} - \alpha u_{yy} - u_x - 2u_y = 0$ | $u_{xx} - 3u_{xy} - 4u_{yy} - u_x - 2u_y = 0$ | ✅ 高 — 具体系数 |
| 四 | $u_{tt}=c^2 u_{xx}+t\sin(x/2)$ | $u_{tt}=a^2 u_{xx}+t\sin(\pi x/L)$ | ✅ 高 |
| 七 | $y''-3y'+\omega^2 y=f(t)$ | $y''-3y'+2y=e^t\sin t$ | ✅ 高 — 具体系数和源项 |
| 八 | 存在不确定性（是否独立一题） | 确认为独立大题（第8题） | ✅ 高 |
| 一.2 | 特征值问题 | 相同，确认 | ✅ 高 |
| 一.5 | Bessel 方程 | 相同，确认 | ✅ 高 |
| 三 | $u_t=u_{xx}-2$, $u(x,0)=x^2$ | 相同，确认 | ✅ 高 |
| 五 | $\Delta u=-4$, $u\|_{r=1}=0$ | 相同，确认 | ✅ 高 |
| 六 | $u(x,0)=1+\cos x$ | 相同，确认 | ✅ 高 |

### 保留不变的部分

| 题号 | 内容 | 说明 |
| ---- | ---- | ---- |
| 一.1 | 极坐标 Laplace | 两版一致 |
| 一.3 | 第二边值必要条件 | 两版一致 |

> 试卷总分构成：填空题 30 分 + 第二~八题（7 × 10 = 70 分）= **100 分**。
